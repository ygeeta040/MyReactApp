//Always write it inside the component or funtion
//component name must be PascalCase(first letter should be Uppercase)
//we can directly import or we can directlywrite it using React.hookName.
//Don't call Hooks inside loops, conditions, or nested funtions.


import React from 'react'

const RulesHooks = () => {
  return (
    <div>
        <h1>RulesHooks</h1>
    </div>
  )
}

export default RulesHooks;